# CueClaw Configuration
# Copy to ~/.cueclaw/config.yaml and fill in your API key
# See docs/config.md for all options

claude:
  api_key: ${ANTHROPIC_API_KEY}
  # base_url: https://api.anthropic.com
  planner:
    model: claude-sonnet-4-6
  executor:
    model: claude-sonnet-4-6

# identity:
#   name: your-name

# whatsapp:
#   enabled: false
#   auth_dir: ~/.cueclaw/auth/whatsapp
#   allowed_jids: []

# telegram:
#   enabled: false
#   token: ${TELEGRAM_BOT_TOKEN}
#   allowed_users: []

logging:
  level: info
  dir: ~/.cueclaw/logs

# container:
#   enabled: false
#   image: cueclaw-agent:latest
#   timeout: 1800000
#   network: none
