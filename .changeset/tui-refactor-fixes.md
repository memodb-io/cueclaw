---
"cueclaw": patch
---

Fix stale closure in command dispatch, add stable message ids for React list keys, cap execution output buffer at 200 lines, and extract shared format-utils module to deduplicate formatting helpers across TUI views.
